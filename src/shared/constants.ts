/**
 * App-wide defaults shared across the main process, renderer, and remote page.
 * Keep values here when the same default is needed in more than one layer.
 */

/** Default playback / monitor volume (0..1) used before the user has set one. */
export const DEFAULT_VOLUME = 0.8
